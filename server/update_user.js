const mongoose = require('mongoose'); const User = require('./models/User'); mongoose.connect('mongodb://127.0.0.1:27017/alphacode').then(async () => { await User.updateOne({ userId: '950936' }, { $set: { role: 'king_admin', permissions: ['overview', 'users', 'support', 'news', 'rebate', 'transfers', 'chart'] } }); console.log('Updated user 950936 in DB'); process.exit(0); }).catch(console.error);
